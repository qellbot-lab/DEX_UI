# Round 4 QA：PPT 文案、动效与响应式标题

## 构建

- `npm run build`：通过。
- Vite 转换 2,833 个模块，无构建错误。
- 浏览器控制台：0 条 error / warning。

## 文案

- 源码中搜索 `不是`、`而是`：0 个匹配。
- 源码中搜索标题内固定 `<br>`：0 个匹配。
- 首页、Agent 广场、组队、历史副本、Runtime 与排行榜已接入 PPT 语言体系。
- 页面流程与中文版 UI 草稿保持一致。

## 色彩

- 源码与 design-system 中搜索 Purple、Violet、Magenta、Indigo、Lavender、Lilac、Mauve：0 个匹配。
- 新增动效只使用 Acid、Cyan、Ember、Rose 与中性表面色。

## 响应式

- 桌面端 8 条路由均只有一个 H1，无水平溢出。
- 390px 视口下 8 条路由均只有一个 H1，无水平溢出。
- 360px 视口下首页、Agent 广场、组队、历史副本、排行榜无水平溢出。
- 移动端标题使用人工校准的语义行，桌面版与移动版可独立调整。

## 动效与交互

- 主要 CTA 的指针位置、折射层与扩散波动画已进入激活状态。
- 历史事件卡片按上涨、下跌、结构变化使用不同方向光场。
- 每张历史事件卡片显示 `SIM AGENTS`，数值每 10 秒在固定范围内变化。
- 历史副本“趋势”筛选只保留 `AI 主升浪`，交互验证通过。
- 首页“探索智能体”跳转到 `/agents`，导航验证通过。
- `prefers-reduced-motion` 下持续动画和过渡会被压缩为近零时长。
